# Migration `20200606120503-modeling-ledger--subledger--transaction--line-item-and-account`

This migration has been generated by dohaicuong at 6/6/2020, 12:05:03 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Ledger" (
"id" TEXT NOT NULL  ,
    PRIMARY KEY ("id"))

CREATE TABLE "quaint"."Subledger" (
"id" TEXT NOT NULL  ,"ledgerId" TEXT   ,
    PRIMARY KEY ("id"),FOREIGN KEY ("ledgerId") REFERENCES "Ledger"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE TABLE "quaint"."Transaction" (
"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"id" TEXT NOT NULL  ,
    PRIMARY KEY ("id"))

CREATE TABLE "quaint"."LineItem" (
"accountId" TEXT NOT NULL  ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"id" TEXT NOT NULL  ,"transactionId" TEXT NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("accountId") REFERENCES "Account"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("transactionId") REFERENCES "Transaction"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Account" (
"id" TEXT NOT NULL  ,"subledgerId" TEXT   ,
    PRIMARY KEY ("id"),FOREIGN KEY ("subledgerId") REFERENCES "Subledger"("id") ON DELETE SET NULL ON UPDATE CASCADE)

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."World";;
PRAGMA foreign_keys=on

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200606120046-init..20200606120503-modeling-ledger--subledger--transaction--line-item-and-account
--- datamodel.dml
+++ datamodel.dml
@@ -1,14 +1,42 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
 }
-     
-model World {
-  id         Int    @id @default(autoincrement())
-  name       String @unique
-  population Float
-}
+
+model Ledger {
+  id         String      @id @default(cuid())
+  subledgers Subledger[]
+}
+
+model Subledger {
+  id       String    @id @default(cuid())
+  accounts Account[]
+  Ledger   Ledger?   @relation(fields: [ledgerId], references: [id])
+  ledgerId String?
+}
+
+model Transaction {
+  id        String     @id @default(cuid())
+  lineItems LineItem[]
+  createdAt DateTime   @default(now())
+}
+
+model LineItem {
+  id            String      @id @default(cuid())
+  account       Account     @relation(fields: [accountId], references: [id])
+  accountId     String
+  transaction   Transaction @relation(fields: [transactionId], references: [id])
+  transactionId String
+  createdAt     DateTime    @default(now())
+}
+
+model Account {
+  id          String     @id @default(cuid())
+  lineItems   LineItem[]
+  Subledger   Subledger? @relation(fields: [subledgerId], references: [id])
+  subledgerId String?
+}
```


