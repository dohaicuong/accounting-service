# Migration `20200606124242-account-update-name-description`

This migration has been generated by dohaicuong at 6/6/2020, 12:42:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Ledger" (
"description" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,
    PRIMARY KEY ("id"))

INSERT INTO "quaint"."new_Ledger" ("id") SELECT "id" FROM "quaint"."Ledger"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Ledger";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Ledger" RENAME TO "Ledger";

CREATE TABLE "quaint"."new_Subledger" (
"description" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"ledgerId" TEXT   ,"name" TEXT NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("ledgerId") REFERENCES "Ledger"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Subledger" ("id", "ledgerId") SELECT "id", "ledgerId" FROM "quaint"."Subledger"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Subledger";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Subledger" RENAME TO "Subledger";

CREATE TABLE "quaint"."new_Account" (
"description" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,"subledgerId" TEXT   ,
    PRIMARY KEY ("id"),FOREIGN KEY ("subledgerId") REFERENCES "Subledger"("id") ON DELETE SET NULL ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Account" ("id", "subledgerId") SELECT "id", "subledgerId" FROM "quaint"."Account"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Account";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Account" RENAME TO "Account";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200606124028-subledger-update-name-description..20200606124242-account-update-name-description
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -22,8 +22,17 @@
   Ledger      Ledger?   @relation(fields: [ledgerId], references: [id])
   ledgerId    String?
 }
+model Account {
+  id          String     @id @default(cuid())
+  name        String
+  description String
+  lineItems   LineItem[]
+  Subledger   Subledger? @relation(fields: [subledgerId], references: [id])
+  subledgerId String?
+}
+
 model Transaction {
   id        String     @id @default(cuid())
   lineItems LineItem[]
   createdAt DateTime   @default(now())
@@ -36,11 +45,4 @@
   transaction   Transaction @relation(fields: [transactionId], references: [id])
   transactionId String
   createdAt     DateTime    @default(now())
 }
-
-model Account {
-  id          String     @id @default(cuid())
-  lineItems   LineItem[]
-  Subledger   Subledger? @relation(fields: [subledgerId], references: [id])
-  subledgerId String?
-}
```


